import { UserMenuContainer } from './UserMenu.styled';

export const UserMenu = () => {
  return (
    <UserMenuContainer>
      <p>UserEmail</p>
      <button type="button">Log out</button>
    </UserMenuContainer>
  );
};
